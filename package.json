{
  "scripts": {
    "parcel:dev": "parcel src/index.html",
    "parcel:build": "parcel build src/index.html",
    "clean": "rimraf dist .parcel-cache .cache-loader",
    "dev": "npm-run-all clean parcel:dev",
    "build": "npm-run-all clean parcel:build",
    "htmlvalidate": "html-validate node_modules/.bin/html-validate src/**/*.html"
  },
  "devDependencies": {
    "autoprefixer": "^9.8.8",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.0.1",
    "parcel-plugin-imagemin": "^4.0.2",
    "postcss-custom-selectors": "^6.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "html-validate": "^6.1.4"
  }
}
